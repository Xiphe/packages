{
  "name": "packages",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r --parallel build",
    "test": "vitest"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@vitest/coverage-v8": "3.0.5",
    "typescript": "5.7.3",
    "vitest": "3.0.5"
  }
}
